#include <cmath> 
#include <utility>

namespace AlgebraicOperations
{
    double dotProduct(const double& dx1, const double& dx2, const double& dy1, const double& dy2)
    {
        return dx1 * dx2 + dy1 * dy2;
    }
};
